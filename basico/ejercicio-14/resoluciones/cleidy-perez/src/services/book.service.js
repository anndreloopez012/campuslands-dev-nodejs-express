function validateBook(book) { const errors = []; if (!book || typeof book !== 'object') return ['El libro debe ser un objeto']; if (!book.title?.trim()) errors.push('title es obligatorio'); if (!book.author?.trim()) errors.push('author es obligatorio'); if (!Number.isInteger(book.year) || book.year < 0 || book.year > new Date().getFullYear()) errors.push('year debe ser un entero válido'); if (!Number.isInteger(book.pages) || book.pages < 1) errors.push('pages debe ser mayor que cero'); return errors; }
function normalizeBook(book) { const errors = validateBook(book); if (errors.length) throw new Error(errors.join('; ')); return { title: book.title.trim(), author: book.author.trim(), year: book.year, pages: book.pages }; }
module.exports = { validateBook, normalizeBook };
